environment: staging

replicaCount: 1

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: Always

resources:
  limits:
    cpu: 750m
    memory: 512Mi
  requests:
    cpu: 300m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
